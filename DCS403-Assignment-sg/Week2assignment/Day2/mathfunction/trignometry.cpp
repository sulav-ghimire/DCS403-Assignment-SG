#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double angle;
    cout << "Enter angle in radians: ";
    cin >> angle;

    cout << "sin = " << sin(angle) << endl;
    cout << "cos = " << cos(angle) << endl;
    cout << "tan = " << tan(angle) << endl;

    return 0;
}

